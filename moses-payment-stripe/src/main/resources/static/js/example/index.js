$(function () {
    // initCustomerScroll(1);
})

function initCustomerScroll(num, links, names) {
    var links = ['using', 'single',];
    var names = ['using-webhooks', 'Stripe Checkout Sample'];
    initScroll("/img/stripe/accordion/", num, links, names);
}
